# M0 intentionally starts provider-agnostic.
# Add provider keys only when their adapters are implemented.
PROBE_TOKEN=
# Used only when invoking `npm run test:railway:smoke` against a deployed probe.
SAVEPLACE_PROBE_URL=
OPENAI_API_KEY=
PLACES_API_KEY=
